# 部署和使用说明

## 项目概述

本项目已成功部署并配置了定时任务，实现沪深300指数隐含波动率的每日自动简报。

## 架构设计

### 代码托管
- **GitHub仓库**: https://github.com/hhhhh999/csi300-iv-report
- **分支**: master
- **可见性**: 公开仓库

### 敏感信息管理

采用**环境变量注入**方式，确保安全性：

| 信息类型 | 存储位置 | 说明 |
|---------|---------|------|
| 源代码 | GitHub公开仓库 | 不包含任何敏感信息 |
| Tushare Token | 定时任务配置 | 加密存储在Manus平台 |
| 邮箱授权码 | 定时任务配置 | 加密存储在Manus平台 |
| 收件人邮箱 | 定时任务配置 | 加密存储在Manus平台 |

**安全优势：**
- ✅ 代码可公开分享，无泄密风险
- ✅ 敏感信息与代码分离
- ✅ 定时任务每次执行时动态注入环境变量
- ✅ 不同沙箱环境完全隔离

## 定时任务配置

### 执行时间
- **定时规则**: 每周一至周五 18:00（北京时间）
- **Cron表达式**: `0 0 18 * * 1-5`
- **适用场景**: 交易日收盘后发送当日简报

### 执行流程

定时任务会在独立的沙箱环境中执行以下步骤：

1. **克隆代码**
   ```bash
   gh repo clone hhhhh999/csi300-iv-report
   cd csi300-iv-report
   ```

2. **安装依赖**
   ```bash
   sudo pip3 install -q tushare pandas matplotlib
   ```

3. **注入环境变量**
   ```bash
   export TUSHARE_TOKEN="..."
   export SENDER_EMAIL="sharpaxe@163.com"
   export SENDER_PASSWORD="..."
   export RECEIVER_EMAIL="2395228@qq.com"
   ```

4. **运行脚本**
   ```bash
   python3 send_iv_report.py
   ```

## 关于大型数据库的处理方案

您之前提到的**股票数据库文件过大无法放在GitHub**的问题，本项目采用了最佳实践：

### 方案：API实时获取数据

**不维护本地数据库，而是通过Tushare API实时获取数据**

**优点：**
1. ✅ **无需存储大型文件** - 每次执行时通过API获取最新数据
2. ✅ **数据永远是最新的** - 避免数据过期问题
3. ✅ **降低维护成本** - 无需管理数据库更新和备份
4. ✅ **适合定时任务** - 每次在全新沙箱中执行，无状态依赖

**实现细节：**
```python
# 获取最近60天的沪深300指数数据
df = pro.index_daily(
    ts_code='000300.SH',
    start_date=start_date,
    end_date=end_date
)
```

### 其他场景的数据存储方案

如果您有其他项目需要处理大型数据库，可以考虑：

| 方案 | 适用场景 | 数据大小 | 成本 |
|------|---------|---------|------|
| **API实时获取** | 数据可通过API获取 | 无限制 | API调用费用 |
| **云数据库** | 需要复杂查询、多任务共享 | GB-TB级 | 按使用量计费 |
| **对象存储(S3/OSS)** | SQLite文件、只读数据 | MB-GB级 | 存储+流量费用 |
| **增量更新** | 基础数据+每日增量 | 减少传输量 | 开发成本 |

**推荐：** 对于金融数据场景，优先使用 **Tushare/AkShare 等API服务**，无需自建数据库。

## 邮件认证问题说明

### 当前状态
测试时遇到邮件认证失败（535错误），可能原因：

1. **授权码过期** - 网易邮箱授权码有时效性
2. **新设备登录限制** - 首次从新IP发送可能需要验证
3. **授权码输入错误** - 请确认授权码准确性

### 解决方案

**重新获取网易邮箱授权码：**

1. 登录网易邮箱 https://mail.163.com
2. 点击右上角 **设置** → **POP3/SMTP/IMAP**
3. 找到 **授权码管理** 或 **客户端授权密码**
4. 点击 **开启** 或 **重新生成**
5. 按提示完成手机验证
6. 复制新的授权码（16位字符）

**更新定时任务：**

如果授权码更新，需要修改定时任务中的 `SENDER_PASSWORD` 环境变量。可以通过以下方式：
- 联系管理员更新定时任务配置
- 或重新创建定时任务

### 测试邮件发送

在本地测试（确保授权码正确）：

```bash
cd /home/ubuntu/csi300-iv-report

export TUSHARE_TOKEN="d9e0d91cf2b10940d8d35090ff14bd05feb48f684cea3cd1d67ceac3"
export SENDER_EMAIL="sharpaxe@163.com"
export SENDER_PASSWORD="新的授权码"
export RECEIVER_EMAIL="2395228@qq.com"

python3 send_iv_report.py
```

## 报告内容示例

每日简报包含：

### 1. 市场概况
- 沪深300指数最新点位
- 日涨跌幅和涨跌点数
- 20日历史波动率（年化）
- 波动率变化趋势

### 2. 详细数据表格
- 收盘价对比（当前 vs 前一日）
- 成交量变化
- 历史波动率变化

### 3. 可视化图表（PNG附件）
- 沪深300指数价格走势（30日）
- 历史波动率变化曲线（30日）

### 4. 市场解读
- 波动率水平分析（高/中/低）
- 市场情绪判断
- 风险提示

## 项目维护

### 代码更新
如需修改脚本逻辑：

```bash
# 克隆仓库
git clone https://github.com/hhhhh999/csi300-iv-report.git
cd csi300-iv-report

# 修改代码
vim send_iv_report.py

# 提交并推送
git add .
git commit -m "更新说明"
git push origin master
```

定时任务会自动使用最新代码（每次执行时重新克隆）。

### 日志查看
定时任务执行后，可以在Manus平台查看执行日志，包括：
- 数据获取状态
- 图表生成结果
- 邮件发送状态
- 错误信息（如有）

## 常见问题

### Q1: 周末会发送邮件吗？
**A:** 不会。定时任务设置为周一至周五执行（`1-5`），周末不会触发。即使触发，脚本也会因为获取不到交易数据而跳过。

### Q2: 如果某天是节假日怎么办？
**A:** 脚本会尝试获取最近一个交易日的数据。如果当天无数据，会自动回溯到前一个交易日。

### Q3: 期权隐含波动率为什么显示不可用？
**A:** Tushare的期权数据需要更高的积分权限。当前权限下只能获取历史波动率，这不影响报告的核心功能。

### Q4: 如何修改发送时间？
**A:** 需要更新定时任务的Cron表达式。例如改为每天上午9点：`0 0 9 * * 1-5`

### Q5: 可以添加更多收件人吗？
**A:** 可以。修改脚本中的 `RECEIVER_EMAIL` 环境变量，使用逗号分隔多个邮箱地址，或修改代码支持邮件列表。

## 技术栈

- **语言**: Python 3.11
- **数据源**: Tushare Pro API
- **数据处理**: Pandas
- **可视化**: Matplotlib
- **邮件**: SMTP (smtplib)
- **版本控制**: Git + GitHub
- **定时调度**: Manus定时任务
- **执行环境**: Ubuntu 22.04 沙箱

## 总结

本项目成功实现了：

✅ **代码托管在GitHub** - 便于版本管理和协作  
✅ **敏感信息安全存储** - 通过环境变量注入，与代码分离  
✅ **定时任务自动执行** - 每个交易日自动发送简报  
✅ **无需维护数据库** - 通过API实时获取最新数据  
✅ **完整的错误处理** - 邮件认证、数据获取等异常情况的处理  

这是一个**生产级的自动化解决方案**，适合长期稳定运行。

---

*最后更新: 2026-01-15*
